// let p=document.querySelector('p')
// console.log(p.classList);
// console.log(p.getAttribute('class'))
// console.log(p.setAttribute('class','newclass'))  ///it is remove old class property
//  console.log(p.classList.add('newclass')) //it is add new class with previous class
// p.classList.remove('class','newclass') // it is remove new class 
// console.log(p.classList); //it is return number of classes i

// console.dir(p) ///it returens the object
// console.log(p);// it returnx the element of the html


//<!--------------------------------------------------------------------------!>
///////////////  append,prepend,after,before

// let div=document.querySelector('div')
//  let btn1=document.createElement('button')
//  let btn2=document.createElement('button')
// btn1.innerText='Click '  // it returns value 
// btn2.innerText='click me'
// div.append(btn1,btn2)   //it add multiple value inside the node(tag) at the end
// // div.appendChild(btn1,btn2)   //it add only value inside the node(tag) at the end
// // div.prepend(btn)  // it add the value inside the node(tag) at the start

// let h1=document.createElement('h1')

// h1.innerHTML='<i>After/Before</i>'  // it returns the value with tags
// //div.after(h1)  /// it add the value after  the node (outside)
// div.before(h1)/// it add the value Before  the node (outside)

// let h5=document.querySelector('h5')
// // h5.remove()  // it remove the node


//<!------------------------------------------------------------------------------>
/////////////////Attributes

// let h2=document.querySelector('h2')
// console.log(h2.getAttribute('class'));  //it returns the attribute
// let h1=document.querySelector('h1')     /// it set the attribute
// console.log(h1.setAttribute('id','IdName'));

